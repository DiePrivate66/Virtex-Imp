"""Legacy compatibility package for the historical public ordering entrypoint.

Canonical public PWA surface:

- ``pos.presentation.api.public``
- ``pos.presentation.api.urls``

This package stays only while older imports still reference ``pedidos.*``.
"""
